import React from "react";

export default function OnlineStores({ product }) {
  return (
    <div>
      online stores list
      {/* {(product || dataNotFound) && (
        <AddOnlineStore dataNotFound={dataNotFound} />
      )} */}
    </div>
  );
}
